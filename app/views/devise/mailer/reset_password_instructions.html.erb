<p>こんにちは <%= @resource.email %>!</p>

<p>パスワード変更のリンクです。</p>

<p><%= link_to 'パスワード再設定URL', edit_password_url(@resource, reset_password_token: @token) %></p>

<p>このメールに心当たりのない場合は無視してください。</p>
<p>上記のリンクにアクセスして新しいパスワードを作成するまで、パスワードの変更はされません。</p>
